#include <stdio.h>

int main() {
	int thisisavariable = 21;
		//int is for integers
	printf("The variable is %d.\n", thisisavariable);
		//%d is to print an integer, specify variable outside quotes
	return 0;
}
