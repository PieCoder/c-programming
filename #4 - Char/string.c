#include <stdio.h>

int main() {
	char string[] = "I am a string; an array of characters";
	printf("%s\n", string);
	//use %s for strings

	printf("The fourth value of the string is %c\n", string[5]);
	return 0;
}
